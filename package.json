{
  "name": "tac-express",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "audit:auth-boundary": "node scripts/audit-auth-boundary.mjs",
    "audit:governance": "node scripts/audit-governance.mjs",
    "audit:design-spec": "node scripts/audit-design-spec.mjs",
    "supabase:types": "supabase gen types typescript --local > packages/database/src/database.types.ts",
    "supabase:reset": "supabase db reset",
    "supabase:start": "supabase start",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "e2e:smoke": "playwright test --project=smoke",
    "e2e:a11y": "playwright test --project=a11y",
    "e2e:visual": "playwright test --project=visual-light --project=visual-dark",
    "e2e:visual:update": "playwright test --project=visual-light --project=visual-dark --update-snapshots",
    "audit:skills": "pnpm dlx tsx scripts/audit-skills.ts",
    "test:routing-eval": "pnpm dlx tsx scripts/test-routing-eval.ts",
    "measure:bundle": "node scripts/measure-bundle-size.mjs",
    "audit:all": "pnpm audit:governance && pnpm audit:auth-boundary && pnpm audit:skills && pnpm audit:design-spec && pnpm test:routing-eval"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@vitejs/plugin-react": "^6.0.1",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "happy-dom": "^20.9.0",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "tsx": "^4.21.0",
    "turbo": "^2.9.6",
    "typescript": "5.9.3",
    "vitest": "^4.1.5"
  },
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@sentry/cli",
      "core-js-pure",
      "esbuild",
      "msw",
      "sharp"
    ]
  }
}
